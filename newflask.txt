from flask import Flask, render_template, request, jsonify
from cleaner.create_json import take_div_num_to_create_json
app = Flask(__name__,
            static_folder='D:\Projects\TT\static',
            template_folder='D:\\Projects\\TT\\html')


@app.route('/')
def index():
    return render_template('index.html', title='TT')


@app.route('/your/flask/route', methods=['POST'])
def return_json_data():
    value = request.form.get('value')
    json_data = take_div_num_to_create_json(value)
    # do something with the value

    return jsonify(json_data)


if __name__ == '__main__':
    app.run()
